import 'package:flutter/material.dart';

class PantallaPedidos extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Aquí van los pedidos del usuario.',
        style: TextStyle(fontSize: 22),
      ),
    );
  }
}
